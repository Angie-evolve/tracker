# Supabase
.branches
.temp

# dotenvx
.env.keys
.env.local
.env.*.local

# La config completa del proyecto no va al repo: es publico, y ahi quedan
# a la vista cosas como que proveedores estan activos y con que limites.
# Se regenera cuando haga falta con: supabase config pull
config.toml
